<%= form_for(@patients_receipt) do |f| %>
...
  <table class="table table-striped table-bordered table-hover">
    <tbody>
      <tr>
         <th>支払日</th>
         <th>支払う人</th>
         <th>サービス名</th>
         <th>取得ポイント</th>
         <th>支払金額</th>
         <th>支払方法</th>
         <th>操作</th>
      </tr>
      <%= f.fields_for :receipts do |q| %>

        <%= render "receipt_fields", f: q %>

      <% end %>
    </tbody>
  </table>
      <div class="links">
      <%= link_to_add_association 'Add new', f, :receipts %>
      </div>

      <%= f.submit %>

<% end %>
